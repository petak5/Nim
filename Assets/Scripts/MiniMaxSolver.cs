using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MiniMaxSolver : MonoBehaviour
{
    public static (int, int) getNextMove(int[] gameState)
    {
        int row = 0;
        int count = 0;

        // TODO: implement minimax searching

        return (row, count);
    }
}
